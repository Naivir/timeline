export const TIMELINE_CONFIG = {
  // Increase/decrease this to control the minimum spacing between visible labels/ticks.
  minTickGapPx: 160,
  // Edge fade intensity zone width from each side.
  edgeFadeZonePx: 110,
  // Extra offscreen render area so labels/ticks can fade in/out smoothly.
  edgeRenderPaddingPx: 40
} as const

